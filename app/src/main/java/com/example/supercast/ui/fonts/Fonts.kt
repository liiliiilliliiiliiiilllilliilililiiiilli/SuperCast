// There all the fonts are contained :]

package com.example.supercast.ui.fonts

import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.Font
import com.example.supercast.R



class Fonts () {

    companion object {

        val ArchivoRegular = FontFamily (Font (R.font.archivo_regular))
        val ArchivoExtraBold = FontFamily (Font (R.font.archivo_extra_bold))
        val ArimoRegular = FontFamily (Font (R.font.arimo_regular))
        val ArimoSemiBold = FontFamily (Font (R.font.arimo_semi_bold))
        val ArimoBold = FontFamily (Font (R.font.arimo_bold))
        val FiraSansSemiBold = FontFamily (Font (R.font.fira_sans_semi_bold))
        val FredokaSemiBold = FontFamily (Font (R.font.fredoka_semi_bold))
        val GadugiRegular = FontFamily (Font (R.font.gadugi_normal))
        val GadugiBold = FontFamily (Font (R.font.gadugi_bold))

    }

}